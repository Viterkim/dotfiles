gui:
  # Keep LazyGit clicks and wheel scrolling. Hold Shift while dragging to select
  # terminal text instead (Windows Terminal/Ghostty, including over SSH).
  mouseEvents: true

os:
  openLink: '+open-link.sh {{link}}'
  copyToClipboardCmd: 'printf %s {{text}} | +clipcopy.sh'
